<div class="cover">
  <div class="container-form">
    <h1> Gagnez de l'argent en louant votre statue !</h1>

    <%= simple_form_for @statue do |f| %>

      <%= f.input :name %>
      <%= f.input :category %>
      <%= f.input :surface %>
      <p>Write a short description for your statue 🗿</p>
      <%= f.text_area :description %>
      <%= f.input :price %>
      <p>📸</p><%= f.input :photo %>
      <%= f.input :photo_cache, as: :hidden %>
      <p class="btn-ghost"><%= f.button :submit %></p>
    <% end %>
  </div>
</div>
