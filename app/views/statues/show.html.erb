<div class="row justify-content-center">
  <div class="show-card">
  <h1> <%= @statue.name %></h1>
  <ul>
    <%= cl_image_tag @statue.photo, height: 300, width: 400, crop: :fill %>
    <% @statues.each do |statue| %>
      <li>
        <%= statue.name + ' - ' + statue.description %>

      </li>
    <% end %>
  </ul>
  </div>
</div>




 <%#= link_to '', dose_path(dose), method: :delete, :class => "fas fa-trash" %>
